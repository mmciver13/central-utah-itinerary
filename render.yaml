# Render Blueprint for Static Site (optional - can also configure in Dashboard)
# https://render.com/docs/static-sites
services:
  - type: web
    name: central-utah-itinerary
    runtime: static
    buildCommand: npm install && npm run build
    staticPublishPath: dist
